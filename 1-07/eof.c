#include <stdio.h>

/* print the value of EOF, first it's contens as a character, then it's value as an integer.
 * I've done both, as I was unsure to what data type exercise 1-7 refered to */
main()
{
    printf("value of EOF as a character:\n");
    printf("%c\n", EOF);
    printf("value of EOF as an integer:\n");
    printf("%d\n", EOF);
}
